CLONE (GET NEW) GIT REPOSITORY ----------------------
git clone https://github.com/YOUR-USERNAME/YOUR-REPOSITORY


UPDATE LOCAL FILE FROM GIT --------------------------
git pull

TRASH LOCAL CHANGES --------------------------
git stash

UPDATING GIT ----------------------------------------
git status
git add .
git commit -m "m"
git push origin main


GIT IGNORE -----------------------------
git rm -r --cached dist
// this line seemed to work = but didn't work for .chache files

------- user funtions still not correct ---------
Need to add code to divert users who hit a url which requires signin 
  = Netlify doesn't allow this anyway. No need for this project.
Need code/page to refresh page when user signs out

------- funtions which need fixing ---------
Quiz modal - needs to close when navigating to the package/find a tour page



