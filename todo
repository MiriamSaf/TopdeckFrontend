CLONE (GET NEW) GIT REPOSITORY ----------------------
git clone https://github.com/YOUR-USERNAME/YOUR-REPOSITORY


UPDATE LOCAL FILE FROM GIT --------------------------
git pull

TRASH LOCAL CHANGES --------------------------
git stash

UPDATING GIT ----------------------------------------
git status
git add .
git commit -m "m"
git push origin main


GIT IGNORE -----------------------------
git rm -r --cached dist
// this line seemed to work

------- user funtions still not correct ---------
Need to add code to divert users who hit a url which requires signin
Need code to refresh page when user sign out


    ---- package is a reserved word? -----
    ${this.packages == null ? html`
          <sl-spinner></sl-spinner>
        ` : html`
          ${this.packages.map(package => html `
              <td-packages class="package-card"
                  title="${package.title}"
                  description="${package.description}"
                  price=${package.price}
                  image="${package.image}"
                >
            </td-packages>
          `)}
        `}


